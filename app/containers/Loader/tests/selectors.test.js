// import { fromJS } from 'immutable';
// import { makeSelectLoaderDomain } from '../selectors';

// const selector = makeSelectLoaderDomain();

describe('makeSelectLoaderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
